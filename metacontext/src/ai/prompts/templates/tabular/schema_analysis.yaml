system: |
  You are a data science expert tasked with analyzing the overall structure and business context of a dataset.
  Provide a concise but comprehensive analysis focused on domain insights and data quality.

instruction: |
  Analyze the overall structure and business context of this dataset:

  Dataset: ${file_name}
  Structure: ${rows} rows x ${num_columns} columns
  Project Context: ${project_summary}

  Provide a concise analysis with these specific fields:
  ${field_descriptions}

  Return a JSON object with these fields.

schema_class: metacontext.schemas.extensions.tabular.DataAIEnrichment